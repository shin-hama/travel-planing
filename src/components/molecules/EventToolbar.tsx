import * as React from 'react'
import Stack from '@mui/material/Stack'

type Props = {
  test?: string
}
const EventToolbar: React.FC<Props> = ({ test }) => {
  return <Stack>test</Stack>
}

export default EventToolbar
